import {
  R,
  destroy,
  getInstance,
  deepClone,
  getComponentInfo,
  setFile,
  destroyAll,
} from './render';
import { findComponent } from './findComponent';
import register from './register';

export {
  R,
  destroy,
  getInstance,
  deepClone,
  findComponent,
  register,
  getComponentInfo,
  setFile,
  destroyAll,
};
