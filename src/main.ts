import {
  openWidget,
  closeWidget,
  getActiveWidgets,
  deepClone,
  getWidgetInfo,
  setWidgetFile,
  closeAllWidgets,
} from './widget';
import { findComponent } from './findComponent';
import registerWidgets from './registerWidgets';

export {
  openWidget,
  closeWidget,
  getActiveWidgets,
  deepClone,
  findComponent,
  registerWidgets,
  getWidgetInfo,
  setWidgetFile,
  closeAllWidgets,
};
